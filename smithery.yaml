version: 1
name: alchemy-studio-mcp
type: mcp
language: node
build:
  dockerfile: Dockerfile
start:
  command: node dist/standalone.js
transport: stdio
env:
  - GEMINI_API_KEY
  - OPENAI_API_KEY
  - OPENAI_MODEL